{
  "development_commands": {
    "file_operations": [
      {
        "trigger": ["read file", "open file", "show file"],
        "action": "read_file",
        "description": "Read and analyze a file from the current project"
      },
      {
        "trigger": ["edit file", "modify file", "change file"],
        "action": "edit_file", 
        "description": "Edit a specific file in the project"
      },
      {
        "trigger": ["create file", "new file", "make file"],
        "action": "create_file",
        "description": "Create a new file in the project"
      }
    ],
    "terminal_commands": [
      {
        "trigger": ["run command", "execute", "run in terminal", "run", "execute command", "terminal command", "command"],
        "action": "send_command",
        "description": "Execute a command in Claude Code terminal"
      },
      {
        "trigger": ["type in terminal", "add text", "write in terminal", "type", "write", "add", "input"],
        "action": "add_text",
        "description": "Add text to Claude Code terminal without executing"
      },
      {
        "trigger": ["press enter", "send", "execute that", "run that", "enter", "go", "submit"],
        "action": "press_enter",
        "description": "Press Enter to execute the current terminal input"
      },
      {
        "trigger": ["show terminal", "terminal output", "what's in terminal", "output", "show output", "terminal"],
        "action": "get_output",
        "description": "Get recent terminal output"
      }
    ],
    "development_helpers": [
      {
        "trigger": ["debug this", "fix error", "help with error"],
        "action": "debug_assistance",
        "description": "Analyze current errors and provide debugging help"
      },
      {
        "trigger": ["explain code", "what does this do", "analyze code"],
        "action": "code_explanation",
        "description": "Explain the current code or recent changes"
      },
      {
        "trigger": ["run tests", "test this", "check tests"],
        "action": "run_tests",
        "description": "Run project tests and analyze results"
      },
      {
        "trigger": ["git status", "check git", "git info"],
        "action": "git_status",
        "description": "Check git repository status"
      },
      {
        "trigger": ["install dependencies", "npm install", "pip install"],
        "action": "install_deps",
        "description": "Install project dependencies"
      }
    ],
    "project_analysis": [
      {
        "trigger": ["project summary", "what's happening", "development status"],
        "action": "project_summary",
        "description": "Get summary of recent development activity"
      },
      {
        "trigger": ["recent files", "what files", "file list"],
        "action": "recent_files",
        "description": "List recently accessed files"
      },
      {
        "trigger": ["recent commands", "command history", "what commands"],
        "action": "recent_commands", 
        "description": "Show recently executed commands"
      }
    ],
    "claude_summary": [
      {
        "trigger": ["give me an update", "give me the update", "what's the update", "update", "status update", "latest"],
        "action": "claude_latest_response",
        "description": "Get Claude's latest response without code context"
      },
      {
        "trigger": ["what's the latest", "what is the latest", "latest update", "what's new", "any updates"],
        "action": "claude_latest_response",
        "description": "Get Claude's most recent text response"
      },
      {
        "trigger": ["what did claude say", "what claude said", "claude's response", "what was said", "claude response"],
        "action": "claude_latest_response",
        "description": "Retrieve Claude's last message text only"
      },
      {
        "trigger": ["where are we", "current status", "where we at", "status", "progress", "what's happening"],
        "action": "claude_conversation_summary",
        "description": "Get a summary of the current conversation context"
      }
    ],
    "conversation_starters": [
      {
        "trigger": ["hi", "hello", "hey", "good morning", "good afternoon", "good evening"],
        "action": "claude_conversation_summary", 
        "description": "Friendly greeting with current status"
      },
      {
        "trigger": ["help", "what can you do", "commands", "help me"],
        "action": "show_capabilities",
        "description": "Show available voice commands and capabilities"
      }
    ]
  },
  "context_keywords": [
    "code", "debug", "error", "file", "terminal", "command", "run", "fix",
    "edit", "python", "javascript", "npm", "git", "test", "build", "function",
    "variable", "class", "method", "syntax", "import", "export", "module",
    "package", "dependency", "install", "compile", "lint", "format"
  ],
  "response_templates": {
    "command_executed": "✅ Executed: {command}",
    "text_added": "📝 Added to terminal: {text}",
    "file_operation": "📄 {action}: {file}",
    "error_help": "🐛 Error analysis: {analysis}",
    "context_summary": "📊 Development context: {summary}",
    "no_action": "❓ I didn't understand that command. Try 'run command X' or 'read file Y'."
  },
  "safety_filters": {
    "dangerous_commands": [
      "rm -rf", "del /s", "format", "fdisk", "dd if=", "shutdown", "reboot",
      "killall", "pkill -9", "> /dev/", "curl | sh", "wget | sh"
    ],
    "require_confirmation": [
      "git push", "git reset --hard", "npm publish", "pip install --upgrade",
      "docker rm", "docker rmi", "systemctl", "service"
    ]
  }
}